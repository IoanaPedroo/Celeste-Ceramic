import React from 'react';

const Clients = () => {
    return (
        <div>
            <h4>listare clienti, date despre acestia, ce au cumparat, obiecte, modificare, stergere</h4>
        </div>
    );
};

export default Clients;
import React from 'react';

const Reports = () => {
    return (
        <div>
            Here starts the reports.
            we list them and we have the default crud methods available.
            we can also import export print each document.
            ReportTypes - monthly taxes(all that was payed/soled in that month)
                        - salaries(for collaborators - additional data needed regarding taxes)
                        - legal persons monthly invoices report
        </div>
    );
};

export default Reports;
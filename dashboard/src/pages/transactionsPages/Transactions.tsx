import React from 'react';

const Transactions = () => {
    return (
        <div>
            <h4>filtatre dupa data, client, nume client, achitare:partial, neachitat, achitat integral</h4>
            <h4>listare, modificare(client, status achitare), stergere tranzactii proforme, augarea se face automat la inchiderea unei vanzari </h4>
        </div>
    );
};

export default Transactions;